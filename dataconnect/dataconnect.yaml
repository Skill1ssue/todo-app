specVersion: "v1"
serviceId: "cloud-sql-todo"
location: "us-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "cloud-testing-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
